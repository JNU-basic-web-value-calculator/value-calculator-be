spring:
  application:
    name: value-calculator

  datasource:
    url: jdbc:mysql://localhost:3306/value_calculator?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: pw
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: true

# ??? ??? ??
kakao:
  client-id: eky
  redirect-uri: http://localhost:8080/api/auth/kakao/callback
  auth-url: https://kauth.kakao.com
  api-url: https://kapi.kakao.com

# JWT ??
jwt:
  secret: value-calculator-secret-key-make-it-very-long-for-security-at-least-256-bits-long
  access-token-expiration: 3600000  # 1??
  refresh-token-expiration: 604800000  # 7?

logging:
  level:
    org.hibernate.SQL: DEBUG
    com.unit_wiseb.value_calculator: DEBUG

server:
  port: 8080

