services:
  app:
    image: value-calculator:latest
    container_name: value-calculator-app
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - value-calculator-network

networks:
  value-calculator-network:
    driver: bridge